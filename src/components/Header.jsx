import LogInDisplay from "./LogInDisplay"

const Header = () => {
    return <section id="header-container">
        < LogInDisplay />
        <h1 id="welcome-header">NC News</h1>
        <h2 id="strapline">Where devs react to current events</h2>
    </section>
}

export default Header